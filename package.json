{
  "name": "lift-code-marketing-loop",
  "private": true,
  "scripts": {
    "renderer:slideshow": "npm --prefix renderer/slideshow run dev -- --host ::",
    "viewer:hypothesis-tree": "python3 -m viewer.hypothesis_tree.app"
  }
}
